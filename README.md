# 🤖 RAG Chatbot с расширенными возможностями

Профессиональный чат-бот с Retrieval-Augmented Generation (RAG), поддерживающий работу с различными LLM через OpenRouter API и локальную обработку документов.

## 🌟 Возможности

- **Множественная загрузка документов** (PDF, TXT, DOCX, HTML, MD)
- **Загрузка аудио/видео файлов** (MP3, WAV, MP4, MOV) с автоматическим преобразованием в текст
- **Выбор различных моделей LLM** через OpenRouter (Claude, Gemini, Mistral, и др.)
- **Экспорт диалогов** в PDF и JSON с поддержкой кириллицы
- **Работа с контекстом** диалога
- **Отображение источников** ответов
- **Сохранение истории диалогов** в базе данных PostgreSQL
- **Создание профилей пользователей** и управление сессиями
- **Автоматическая загрузка** ранее обработанных документов при перезапуске
- **Использование GPU** (если доступно) для ускорения обработки (Whisper, Embeddings)
- **Упрощенная установка** через скрипты `.bat` (Windows)

## 🚀 Технологии

- **LLM**: OpenRouter API (Claude, Gemini, Mistral, и др.) / Локальные модели
- **Framework**: LangChain
- **Vector Store**: FAISS (CPU)
- **UI**: Gradio
- **Embeddings**: OpenAI Embeddings via OpenRouter / Локальные Sentence Transformers
- **Speech-to-Text**: OpenAI Whisper
- **Video/Audio Processing**: MoviePy, FFmpeg
- **PDF Export**: ReportLab с поддержкой Unicode
- **Database**: PostgreSQL (psycopg2)

## 📦 Установка (Рекомендуемый способ - Windows)

Для пользователей Windows предоставлены скрипты для упрощения установки и запуска.

### 1. Подготовка

1.  Убедитесь, что у вас установлен **Python 3.10 или выше** и он добавлен в `PATH`.
2.  Установите **FFmpeg** одним из следующих способов:
    *   **(Рекомендуется)** `winget install "FFmpeg (Essentials Build)"`
    *   Или вручную: [https://www.gyan.dev/ffmpeg/builds/](https://www.gyan.dev/ffmpeg/builds/) (скачайте `ffmpeg-release-essentials.zip`, распакуйте и добавьте путь к `bin` в переменные среды `PATH`).
3.  Установите **PostgreSQL** и создайте базу данных `rag_chatbot`.

### 2. Установка и запуск

1.  Склонируйте или скачайте репозиторий.
2.  Запустите `install_FFmpeg.bat`. Этот скрипт:
    *   Проверит наличие Python.
    *   Создаст виртуальное окружение `venv`.
    *   Установит все необходимые зависимости из `requirements.txt`.
    *   Проверит установку PyTorch (для GPU, если доступно) и FFmpeg.
3.  После установки запустите `run.bat` для запуска приложения.
4.  Откройте в браузере: `http://localhost:7860`

## 📦 Установка (Универсальный способ)

### 1. Клонирование репозитория

```bash
git clone https://github.com/<ваш_логин>/rag-chatbot.git
cd rag-chatbot

2. Создание виртуального окружения

# Создание виртуального окружения
python -m venv .venv

# Активация (Windows)
.venv\Scripts\activate

# Активация (macOS/Linux)
source .venv/bin/activate

3. Установка зависимостей

pip install -r requirements.txt

4. Установка FFmpeg

Установите FFmpeg в вашу систему и убедитесь, что он доступен в PATH.

Linux (Ubuntu/Debian): sudo apt install ffmpeg
macOS (Homebrew): brew install ffmpeg
Windows: См. инструкцию выше.

5. Настройка API ключа и базы данных

# Скопируйте пример конфигурации
    cp example.env .env # или ren example.env .env (Windows)

Откройте файл .env и настройте параметры:
# API ключ OpenRouter (обязателен для внешних embeddings/LLM)
    OPENROUTER_API_KEY=ваш_ключ_openrouter_здесь

# Настройки базы данных PostgreSQL (обязательны)
    DB_HOST=localhost
    DB_NAME=rag_chatbot
    DB_USER=ваш_пользователь_postgres
    DB_PASSWORD=ваш_пароль_postgres
    DB_PORT=5432

6. Подготовка базы данных PostgreSQL

1. становите PostgreSQL.

2. Создайте базу данных
    CREATE DATABASE rag_chatbot;

(Убедитесь, что кодировка базы данных - UTF8)

▶️ Запуск
    python app.py

Откройте в браузере: http://localhost:7860

📖 Использование

Авторизация:
Введите имя пользователя и нажмите "Войти".

Управление сессиями:
Создайте новую сессию с произвольным названием или выберите существующую сессию из списка.

Загрузка документов:
Загрузите один или несколько файлов (PDF, TXT, DOCX, HTML, MD, MP3, WAV, MP4, MOV).
Нажмите "Обработать документы". Аудио/видео файлы будут автоматически преобразованы в текст.

Инициализация чат-бота:
Выберите желаемую модель LLM из списка.
Нажмите "Инициализировать чат-бота".

Работа с чатом:
Задавайте вопросы по загруженным документам.
Просматривайте источники ответов.

Экспорт диалога:
Используйте кнопки "Экспорт в JSON" или "Экспорт в PDF".
При необходимости укажите директорию для сохранения.

🔧 Поддерживаемые форматы документов
Текстовые: PDF, TXT, DOCX, HTML, MD
Аудио: MP3, WAV
Видео: MP4, MOV
🧠 Использование GPU
Если у вас есть NVIDIA GPU с CUDA, приложение автоматически попытается использовать её для ускорения обработки Whisper и создания embeddings. Убедитесь, что у вас установлены драйверы NVIDIA и CUDA Toolkit.

🗃️ Работа с базой данных
Проект поддерживает сохранение истории диалогов в PostgreSQL базе данных.

(Остальная часть описания БД, структуры проекта, безопасности, вклада, лицензии и благодарностей остается без изменений или обновляется по аналогии)

🎯 Что реализовано:
✅ Сохранение истории диалогов в базе данных PostgreSQL
✅ Возможность продолжать диалоги между сессиями
✅ Создание "профилей" пользователей
✅ Управление сессиями (создание, загрузка, обновление)
✅ Автоматическое обновление времени активности
✅ Индексы для улучшения производительности
✅ Автоматическая загрузка векторного хранилища
✅ Поддержка аудио/видео файлов
✅ Использование GPU (если доступно)

Для работы не забудьте:
    1. Установить PostgreSQL и создать базу данных rag_chatbot.
    2. Установить FFmpeg.
    3. Установить зависимости: pip install -r requirements.txt.
    4. Настроить .env файл с параметрами подключения к БД и API-ключом.

🧪 Тестирование
    # Запуск тестов (если есть)
    pytest tests/

📁 Структура проекта
RAG-CHATBOT-PROJECT/
├── .env                    # Файл конфигурации с секретами (API ключи, настройки БД). Не включается в репозиторий (.gitignore).
├── .gitignore              # Файл, указывающий Git, какие файлы и директории игнорировать (например, .env, venv, logs, data).
├── app.py                  # Главный файл приложения. Запускает Gradio интерфейс и содержит основную логику обработки файлов и чата.
├── check_db.py             # (Возможный) скрипт для проверки подключения к БД или инициализации таблиц.
├── constraints.txt         # (Опционально) Файл для фиксации точных версий зависимостей.
├── example.env             # Пример файла конфигурации (.env) с переменными окружения. Используется как шаблон.
├── install_FFmpeg.bat      # Скрипт установки для Windows: создает venv, устанавливает зависимости из requirements.txt, проверяет FFmpeg.
├── LICENSE                 # Лицензия проекта.
├── README.md               # Документация проекта: описание, возможности, установка, использование.
├── requirements.txt        # Список всех зависимостей Python, необходимых для работы проекта.
├── run.bat                 # Скрипт запуска для Windows: активирует venv и запускает app.py.
├── config/                 # Директория для конфигурационных файлов проекта.
│   ├── __init__.py         # Делает директорию пакетом Python.
│   └── settings.py         # Основной файл настроек проекта, загружает переменные окружения из .env.
├── data/                   # Директория для хранения данных, создаваемых приложением.
│   ├── processed/          # (Возможная) директория для обработанных файлов.
│   ├── raw/                # (Возможная) директория для исходных загруженных файлов.
│   └── vectorstore/        # Директория для хранения векторного индекса FAISS.
├── fonts/                  # Директория для пользовательских шрифтов, используемых при экспорте в PDF (например, DejaVuSans).
├── logs/                   # Директория для файлов логов приложения.
├── migrations/             # Директория для скриптов миграции базы данных.
│   └── init.sql            # SQL-скрипт для инициализации (создания) таблиц в базе данных PostgreSQL.
├── notebooks/              # Директория для Jupyter Notebooks (например, для исследования или прототипирования).
│   └── rag_exploration.ipynb # Пример notebook.
├── src/                    # Директория с основным исходным кодом проекта.
│   ├── __pycache__/        # Кэш скомпилированных Python файлов.
│   ├── __init__.py         # Делает директорию пакетом Python.
│   ├── chat_chain.py       # Определяет цепочку RAG (Retrieval-Augmented Generation) для обработки вопросов.
│   ├── database.py         # Модуль для работы с базой данных PostgreSQL (подключение, сохранение/загрузка сообщений, сессий, пользователей).
│   ├── document_processor.py # Модуль для загрузки и предварительной обработки различных форматов документов (PDF, TXT и др.).
│   ├── embeddings_handler.py # Модуль для получения векторных представлений (embeddings) текста, используя OpenRouter API или локальные модели.
│   ├── export_handler.py   # Модуль для экспорта истории чата в форматы PDF и JSON.
│   └── vector_store.py     # Модуль для работы с векторным хранилищем FAISS (создание, сохранение, загрузка, поиск).
├── tests/                  # Директория для unit-тестов.
│   ├── __init__.py         # Делает директорию пакетом Python.
│   └── test_document_processor.py # Пример теста для модуля обработки документов.
└── utils/                  # Директория для вспомогательных утилит и функций.
    ├── __init__.py         # Делает директорию пакетом Python.
    └── helpers.py          # Файл с вспомогательными функциями.

🔒 Безопасность
API ключи и настройки БД хранятся в файле .env, который не включается в репозиторий.
Все секреты должны быть в .env файле.
Никогда не публикуйте файлы с API ключами и паролями.

🤝 Вклад в проект
Форкните репозиторий
Создайте ветку для вашей функции (git checkout -b feature/AmazingFeature)
Зафиксируйте изменения (git commit -m 'Add some AmazingFeature')
Отправьте изменения в ветку (git push origin feature/AmazingFeature)
Откройте Pull Request

📄 Лицензия
Этот проект лицензирован по лицензии MIT - см. файл LICENSE для подробностей.

🙏 Благодарности
OpenRouter - за предоставление доступа к различным LLM
LangChain - за фреймворк
Gradio - за простой веб-интерфейс
ReportLab - за генерацию PDF
FAISS - за векторное хранилище

📞 Контакты
Если у вас есть вопросы, создайте issue в этом репозитории.

