# 🤖 RAG Chatbot с расширенными возможностями

Профессиональный чат-бот с Retrieval-Augmented Generation (RAG), поддерживающий работу с различными LLM через OpenRouter API и локальную обработку документов.

## 🌟 Возможности

- **Множественная загрузка документов** (PDF, TXT, DOCX, HTML, MD)
- **Загрузка аудио/видео файлов** (MP3, WAV, MP4, MOV) с автоматическим преобразованием в текст
- **Выбор различных моделей LLM** через OpenRouter (Claude, Gemini, Mistral, и др.)
- **Экспорт диалогов** в PDF и JSON с поддержкой кириллицы
- **Работа с контекстом** диалога
- **Отображение источников** ответов
- **Сохранение истории диалогов** в базе данных PostgreSQL
- **Создание профилей пользователей** и управление сессиями
- **Автоматическая загрузка** ранее обработанных документов при перезапуске
- **Использование GPU** (если доступно) для ускорения обработки (Whisper, Embeddings)

## 🚀 Технологии

- **LLM**: OpenRouter API (Claude, Gemini, Mistral, и др.) / Локальные модели
- **Framework**: LangChain
- **Vector Store**: FAISS (CPU)
- **UI**: Gradio
- **Embeddings**: OpenAI Embeddings via OpenRouter / Локальные Sentence Transformers
- **Speech-to-Text**: OpenAI Whisper
- **Video/Audio Processing**: MoviePy, FFmpeg
- **PDF Export**: ReportLab с поддержкой Unicode
- **Database**: PostgreSQL (psycopg2)

## 📦 Установка (Рекомендуемый способ - Docker)

Для удобства развертывания рекомендуется использовать Docker Compose.

### 1. Подготовка

1.  Убедитесь, что у вас установлен **Docker** и **Docker Compose**.
2.  Установите **FFmpeg** одним из следующих способов:
    *   **(Рекомендуется)** `winget install "FFmpeg (Essentials Build)"`
    *   Или вручную: [https://www.gyan.dev/ffmpeg/builds/](https://www.gyan.dev/ffmpeg/builds/) (скачайте `ffmpeg-release-essentials.zip`, распакуйте и добавьте путь к `bin` в переменные среды `PATH`).
3.  Установите **PostgreSQL** и создайте базу данных `rag_chatbot`.

### 2. Установка и запуск

1.  Склонируйте или скачайте репозиторий.
2.  Создайте файл `.env` на основе `example.env`:
    ```bash
    cp example.env .env
    ```
    Откройте файл `.env` и настройте параметры:
    ```env
    # API ключ OpenRouter (обязателен для внешних embeddings/LLM)
    OPENROUTER_API_KEY=ваш_ключ_openrouter_здесь

    # Настройки базы данных PostgreSQL (обязательны)
    DB_HOST=localhost
    DB_NAME=rag_chatbot
    DB_USER=ваш_пользователь_postgres
    DB_PASSWORD=ваш_пароль_postgres
    DB_PORT=5432
    ```

3.  Запустите контейнеры:
    ```bash
    docker-compose up --build
    ```

4.  После запуска откройте в браузере: `http://localhost:7860`

### 3. Экспорт файлов

При экспорте чата в формате PDF или JSON, файлы будут сохраняться по умолчанию в директорию `exports` вашего проекта на хосте. Вы можете указать другую директорию экспорта в поле "Директория для экспорта" в интерфейсе Gradio.

## 📦 Установка (Универсальный способ)

Если вы предпочитаете не использовать Docker, можно установить все зависимости напрямую:

### 1. Клонирование репозитория

    ```bash
    git clone https://github.com/<ваш_логин>/rag-chatbot.git
    cd rag-chatbot
    ```

### 2. Создание виртуального окружения

    ```bash
    python -m venv .venv
    source .venv/bin/activate  # macOS/Linux
    .venv\Scripts\activate      # Windows
    ```

### 3. Установка зависимостей

    ```bash
    pip install -r requirements.txt
    ```

### 4. Установка FFmpeg

Установите FFmpeg в вашу систему и убедитесь, что он доступен в PATH:

    ```bash
    Linux (Ubuntu/Debian): sudo apt install ffmpeg
    macOS (Homebrew): brew install ffmpeg
    Windows: См. инструкцию выше.
    ```

### 5. Настройка API ключа и базы данных

Скопируйте пример конфигурации:

    ```bash
    cp example.env .env
    ```

Откройте файл .env и настройте параметры:

```bash
# API ключ OpenRouter (обязателен для внешних embeddings/LLM)
OPENROUTER_API_KEY=ваш_ключ_openrouter_здесь

# Настройки базы данных PostgreSQL (обязательны)
DB_HOST=localhost
DB_NAME=rag_chatbot
DB_USER=ваш_пользователь_postgres
DB_PASSWORD=ваш_пароль_postgres
DB_PORT=5432
```

### 6. Подготовка базы данных PostgreSQL

```bash
1. Установите PostgreSQL.
2. Создайте базу данных:
    CREATE DATABASE rag_chatbot;

Убедитесь, что кодировка базы данных — UTF8.
```

### 7. Запуск

🐳 Запуск проекта с помощью Docker (Рекомендуемый способ)
Для удобного развертывания и обеспечения согласованной среды выполнения на разных системах проект можно запустить с использованием Docker Compose. Этот метод автоматически создаст и запустит два контейнера: один для приложения и один для базы данных PostgreSQL.

Предварительные требования

Установите Docker и Docker Compose (обычно идут вместе) на ваш компьютер. Инструкции по установке можно найти на официальном сайте Docker .

Шаги для запуска

## 1. Клонирование или загрузка проекта
    Скачайте или клонируйте репозиторий проекта на ваш локальный компьютер.

## 2. Настройка конфигурации
    Скопируйте файл example.env в новый файл с именем .env. Этот файл будет содержать ваши персональные настройки и ключи API.

    ```bash
    cp example.env .env
    ```

Откройте файл .env в текстовом редакторе и укажите ваш OPENROUTER_API_KEY. Остальные настройки БД по умолчанию подходят для конфигурации Docker Compose.

## 3. (Опционально) Инициализация базы данных
    Если таблицы в базе данных ещё не созданы, вы можете автоматически выполнить скрипт инициализации при первом запуске. Для этого убедитесь, что в файле docker-compose.yml раскомментирована следующая строка в сервисе db:

    ```bash
    # - ./migrations/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ```

Или инициализируйте БД вручную после запуска контейнеров (см. шаг 5).

## 4. Запуск контейнеров
Откройте терминал (командную строку) в корневой директории проекта (там, где находится файл docker-compose.yml) и выполните команду:

    ```bash
    docker-compose up --build
    ```

--build: Эта опция указывает Docker Compose пересобрать образ приложения, если файлы проекта изменились.
При первом запуске этот процесс может занять несколько минут, так как Docker будет загружать базовые образы (например, python:3.10-slim, postgres:15) и устанавливать все зависимости Python внутри контейнера.


## 5. (Если не использовали автоматическую инициализацию) Инициализация структуры БД вручную
Если вы не активировали автоматическую инициализацию через docker-compose.yml, после запуска контейнеров (когда они будут в статусе Up) откройте новое окно терминала и выполните:

    ```bash
    docker-compose exec app python init_db.py
    ```

Это выполнит скрипт init_db.py внутри контейнера приложения, который, в свою очередь, запустит выполнение migrations/init.sql и создаст необходимые таблицы в базе данных.

## 6. Доступ к приложению

    После успешного запуска и инициализации контейнеров, откройте веб-браузер и перейдите по адресу:

    ```bash
    http://localhost:7860
    ```

Вы увидите интерфейс Gradio вашего RAG-чатбота.


Экспорт файлов
Файлы, экспортированные из чата (PDF, JSON), по умолчанию сохраняются в директорию exports внутри контейнера. Благодаря настроенному монтированию томов в docker-compose.yml, эти файлы автоматически появятся в локальной папке exports в корне проекта на вашем компьютере. Вы можете изменить директорию экспорта в интерфейсе Gradio, указав другой путь (он должен быть доступен внутри контейнера, например, /app/data).

Остановка проекта
Чтобы остановить контейнеры, нажмите Ctrl+C в окне терминала, где они запущены, или выполните в другом окне терминала:

docker-compose down
Это остановит и удалит контейнеры, но сохранит данные базы данных в именованном томе Docker (postgres_data), чтобы они не потерялись при следующем запуске.

Чтобы остановить контейнеры и удалить все данные (включая данные БД), выполните:
docker-compose down -v
Используйте эту команду с осторожностью.

# 7.1 Запуск без Docker:

    ```bash
    python app.py
    Откройте в браузере: http://localhost:7860
    ```

### 📖 Использование

Авторизация: Введите имя пользователя и нажмите "Войти".
Управление сессиями: Создайте новую сессию с произвольным названием или выберите существующую сессию из списка.
Загрузка документов: Загрузите один или несколько файлов (PDF, TXT, DOCX, HTML, MD, MP3, WAV, MP4, MOV). Нажмите "Обработать документы". Аудио/видео файлы будут автоматически преобразованы в текст.
Инициализация чат-бота: Выберите желаемую модель LLM из списка. Нажмите "Инициализировать чат-бота".
Работа с чатом: Задавайте вопросы по загруженным документам. Просматривайте источники ответов.
Экспорт диалога: Используйте кнопки "Экспорт в JSON" или "Экспорт в PDF". При необходимости укажите директорию для сохранения.
🔧 Поддерживаемые форматы документов

Текстовые: PDF, TXT, DOCX, HTML, MD
Аудио: MP3, WAV
Видео: MP4, MOV
🧠 Использование GPU
Если у вас есть NVIDIA GPU с CUDA, приложение автоматически попытается использовать её для ускорения обработки Whisper и создания embeddings. Убедитесь, что у вас установлены драйверы NVIDIA и CUDA Toolkit.

🗃️ Работа с базой данных
Проект поддерживает сохранение истории диалогов в PostgreSQL базе данных.

🎯 Что реализовано:
✅ Сохранение истории диалогов в базе данных PostgreSQL
✅ Возможность продолжать диалоги между сессиями
✅ Создание "профилей" пользователей
✅ Управление сессиями (создание, загрузка, обновление)
✅ Автоматическое обновление времени активности
✅ Индексы для улучшения производительности
✅ Автоматическая загрузка векторного хранилища
✅ Поддержка аудио/видео файлов
✅ Использование GPU (если доступно)

Для работы не забудьте:

Установить PostgreSQL и создать базу данных rag_chatbot.
Установить FFmpeg.
Установить зависимости: pip install -r requirements.txt.
Настроить .env файл с параметрами подключения к БД и API-ключом.

🧪 Тестирование
pytest tests/

## 📁 Структура проекта

RAG-CHATBOT-PROJECT/
├── .dockerignore # Файл, указывающий Docker, какие файлы и директории игнорировать при сборке образа.
├── .env # Файл конфигурации с секретами (API ключи, настройки БД). Не включается в репозиторий (.gitignore).
├── .gitignore # Файл, указывающий Git, какие файлы и директории игнорировать (например, env, venv, logs, data).
├── app.py # Главный файл приложения. Запускает Gradio интерфейс и содержит основную логику обработки файлов и чата.
├── check_db.py # Скрипт для проверки подключения к БД и просмотра содержимого таблиц.
├── constraints.txt # (Опционально) Файл для фиксации точных версий зависимостей.
├── docker-compose.yml # Файл конфигурации Docker Compose для оркестрации контейнеров приложения и БД.
├── Dockerfile # Файл инструкций для сборки Docker-образа приложения.
├── example.env # Пример файла конфигурации (.env) с переменными окружения. Используется как шаблон.
├── init_db.py # Скрипт для инициализации структуры базы данных PostgreSQL.
├── LICENSE # Лицензия проекта.
├── README.md # Документация проекта: описание, возможности, установка, использование.
├── requirements.txt # Список всех зависимостей Python, необходимых для работы проекта.
├── config/ # Директория для конфигурационных файлов.
│ ├── init.py # Создает директорию пакетом Python.
│ └── settings.py # Основной файл настроек проекта, загружает переменные окружения из .env.
├── data/ # Директория для хранения данных, создаваемых приложением.
│ ├── processed/ # (Возможная) директория для обработанных файлов.
│ │ └── vectorstore/ # Директория для хранения векторного индекса FAISS.
│ └── exports/ # Директория для файлов, экспортированных из чата (PDF, JSON). Создается автоматически.
│ └── fonts/ # Директория для пользовательских шрифтов, используемых при экспорте в PDF (например, DejaVuSans).
├── logs/ # Директория для файлов логов приложения.
├── migrations/ # Директория для скриптов миграции базы данных.
│ └── init.sql # SQL-скрипт для инициализации (создания) таблиц в базе данных PostgreSQL.
├── notebooks/ # Директория для Jupyter Notebooks (например, для исследования или прототипирования).
│ └── rag_exploration.ipynb # Пример notebook.
├── src/ # Директория с основным исходным кодом проекта.
│ ├── init.py # Создает директорию пакетом Python.
│ ├── chat_chain.py # Определяет цепочку RAG (Retrieval-Augmented Generation) для обработки вопросов.
│ ├── database.py # Модуль для работы с базой данных PostgreSQL (подключение, сохранение/загрузка сообщений, сессий, пользователей).
│ ├── document_processor.py # Модуль для загрузки и предварительной обработки различных форматов документов (PDF, TXT и др.).
│ ├── embeddings_handler.py # Модуль для получения векторных представлений (embeddings) текста, используя OpenRouter API или локальные модели.
│ ├── export_handler.py # Модуль для экспорта истории чата в форматы PDF и JSON.
│ ├── llm_handler.py # Модуль для работы с LLM через OpenRouter API или локально.
│ ├── vector_store.py # Модуль для работы с векторным хранилищем FAISS (создание, сохранение, загрузка, поиск).
│ ├── tests/ # Директория для unit-тестов.
│ │ └── test_document_processor.py # Пример теста для модуля обработки документов.
│ └── utils/ # Директория для вспомогательных утилит и функций.
│ └── helpers.py # Файл с вспомогательными функциями.
└── pycache/ # Кеш скомпилированных Python файлов.

🔒 Безопасность
API ключи и настройки БД хранятся в файле .env, который не включается в репозиторий.
Все секреты должны быть в .env файле.
Никогда не публикуйте файлы с API ключами и паролями.

🤝 Вклад в проект
Форкните репозиторий
Создайте ветку для вашей функции (git checkout -b feature/AmazingFeature)
Зафиксируйте изменения (git commit -m 'Add some AmazingFeature')
Отправьте изменения в ветку (git push origin feature/AmazingFeature)
Откройте Pull Request

📄 Лицензия
Этот проект лицензирован по лицензии MIT - см. файл LICENSE для подробностей.

🙏 Благодарности
OpenRouter - за предоставление доступа к различным LLM
LangChain - за фреймворк
Gradio - за простой веб-интерфейс
ReportLab - за генерацию PDF
FAISS - за векторное хранилище

📞 Контакты
Если у вас есть вопросы, создайте issue в этом репозитории.