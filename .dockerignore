# Виртуальное окружение Python
.venv/
venv/
env/

# Временные файлы и кэши
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.pytest_cache/
.coverage

# Системные и скрытые файлы
.git
.DS_Store
Thumbs.db

# Логи
logs/

# Конфигурационные файлы IDE
.idea/
.vscode/

# Файлы установки/запуска Windows (не нужны внутри контейнера)
*.bat

# Персональные данные и секреты
.env
.env.local
.env.*.local
example.env

# Данные приложения (монтируются как тома)
# Это важно: данные в data/ будут в контейнере благодаря volumes,
# но мы не хотим копировать локальные данные внутрь образа при сборке
data/

# Кэш моделей (если монтируется отдельно)
.cache/

# Docker- и Compose- файлы (маленькие, но обычно не нужны внутри образа)
docker-compose.yml
docker-compose.*.yml
Dockerfile

# README и документация (если точно не нужны внутри контейнера)
# README.md
# *.md (раскомментируйте, если не хотите копировать другие .md файлы)

# Тесты (если они есть отдельно и точно не нужны в продакшен-образе)
# tests/